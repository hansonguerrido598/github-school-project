#include <stdio.h>

int main() {
    int x = 5;
    if (x > 3) {
        printf("The number is greater than 3\n");
    } else {
        printf("The number is not greater than 3\n");
    }
    return 0;
}
